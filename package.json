{
  "name": "agent-tools-hub",
  "version": "0.1.0",
  "private": true,
  "description": "A curated directory of skills, MCP servers, plugins, and workflows for AI coding agents.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4322",
    "validate": "python3 -m json.tool data/skills.json > /dev/null && node --check assets/app.js && node --check scripts/analyze-submission.mjs && node scripts/validate.mjs"
  },
  "license": "MIT"
}
